name: sa_flutter_secure_storage_lego
description: "apply flitter_secure_storage to simple architecture framework"
#publish_to: 'none'
repository: https://github.com/flutter-lego/sa_flutter_secure_storage_lego

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: #@add
  gap: #@add
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  flutter_screenutil:
  bot_toast:
  flutter_secure_storage: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  screenutil_lego: #@add
  bot_toast_lego: #@add
  usage_note_lego:
  widget_binding_lego: #@add
  sa_data_class_lego:

flutter:
  assets:
    - assets/lego/sa_flutter_secure_storage_lego/
  uses-material-design: true

topics:
  - lego